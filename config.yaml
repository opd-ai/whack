# Whack - Default Configuration
game:
  title: "Whack"
  width: 800
  height: 600
  tps: 60
  seed: 0
  genre: "fantasy"

server:
  address: ":7000"
  max_players: 4
  tick_rate: 60

client:
  server_address: "localhost:7000"

match:
  stocks: 3
  time_limit: 300
  mode: "ffa"
